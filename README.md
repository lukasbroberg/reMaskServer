# reMaskServer
This contains the server for reMask. The server is essential for reMask to run. 
# How to run
Prerequisites: node.js (install https://nodejs.org/en)
Open the project folder in your preferred IDE.
Type node index.js in the terminal, to start the server.